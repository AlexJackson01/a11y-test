"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.hasInvalidRole = void 0;
const t = __importStar(require("@babel/types"));
const constants_1 = require("../../constants");
function isValidRole(role) {
    if (!role)
        return false;
    // Support either Set<string> or string[]
    const anyRoles = constants_1.ALL_ROLES;
    if (typeof anyRoles.has === "function") {
        return anyRoles.has(role);
    }
    if (Array.isArray(anyRoles)) {
        return anyRoles.includes(role);
    }
    // Fallback (be conservative)
    return false;
}
const hasInvalidRole = (tag, attributes, path) => {
    const roleAttr = attributes.find((attr) => t.isJSXAttribute(attr) &&
        t.isJSXIdentifier(attr.name) &&
        attr.name.name === "accessibilityRole");
    if (!roleAttr || !roleAttr.value) {
        return null;
    }
    let roleValue = null;
    if (t.isStringLiteral(roleAttr.value)) {
        roleValue = roleAttr.value.value;
    }
    else if (t.isJSXExpressionContainer(roleAttr.value) &&
        t.isStringLiteral(roleAttr.value.expression)) {
        roleValue = roleAttr.value.expression.value;
    }
    else {
        // Non-string expressions (e.g., identifier, member expression)
        roleValue = null;
    }
    if (isValidRole(roleValue)) {
        return null;
    }
    return {
        tag,
        path,
        line: path.node.loc?.start.line || 0,
        text: `${constants_1.roleCode} - '${roleValue}' is invalid`,
        existingText: roleValue,
    };
};
exports.hasInvalidRole = hasInvalidRole;
//# sourceMappingURL=hasInvalidRole.js.map