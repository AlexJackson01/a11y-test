"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.hasAriaState = exports.inferFromFilename = exports.inferFromTag = exports.NON_INTERACTIVE = void 0;
const path_1 = __importDefault(require("path"));
const constants_1 = require("../../constants");
const hasProp_1 = require("../hasProp");
exports.NON_INTERACTIVE = new Set(["Text"]);
const inferFromTag = (tag) => {
    return constants_1.stateMap[tag] ?? null;
};
exports.inferFromTag = inferFromTag;
const inferFromFilename = (filePath) => {
    if (!filePath)
        return null;
    const base = path_1.default.basename(filePath).toLowerCase();
    for (const [comp, state] of Object.entries(constants_1.stateMap)) {
        if (base.includes(comp.toLowerCase()))
            return state;
    }
    return null;
};
exports.inferFromFilename = inferFromFilename;
const hasAriaState = (attributes, key) => {
    const aria = key === "checked"
        ? "aria-checked"
        : key === "expanded"
            ? "aria-expanded"
            : key === "selected"
                ? "aria-selected"
                : null;
    if (!aria)
        return false;
    return (0, hasProp_1.hasProp)(aria, attributes);
};
exports.hasAriaState = hasAriaState;
//# sourceMappingURL=suggestState.js.map