import * as t from "@babel/types";

export const hasProp = (name: string, attributes: (t.JSXAttribute | t.JSXSpreadAttribute)[]) =>
    attributes.some(
      (attr) =>
        t.isJSXAttribute(attr) &&
        t.isJSXIdentifier(attr.name) &&
        attr.name.name === name
    );